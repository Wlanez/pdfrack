spec:
  template:
    spec:
      initContainers:
        - name: carga
          command: ["sh", "-c", "chown -R 200:200 /media"]
          volumeMounts:
            - mountPath: /media
              name: volume-9wbrl
            - mountPath: /afp
              name: volume-9wbrl
